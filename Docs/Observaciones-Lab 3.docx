
¿Cuáles son los mecanismos de interacción (I/O: Input/Output) que tiene el view.py con el
usuario?
Los inputs que tiene el view.py son aquellos en los que se le pide al usuario información para continuar el programa.
Los outputs son los resultados arrojados por el programa después de otorgar inputs. 

¿Cómo se almacenan los datos de GoodReads en el model.py?
El model.py almacena los datos en una lista.

¿Cuáles son las funciones que comunican el view.py y el model.py?
Las funciones que comunican el view.py con el model.py son: initCatalog(), loadData(), y el menú.
La comunicación es indirecta, pues estas funciones invocan el controller, nunca el model. Sin embargo, el controller sí invoca al model.

¿Cómo se crea una lista?
Una lista se crea importando la 

¿Qué hace el parámetro cmpfunction=None en la función newList()?


¿Qué hace la función addLast()?
La función addLast() añade un elemento al final de una lista.

¿Qué hace la función getElement()?
La función getElement() arroja el valor del elemento pedido por el usuario. 

¿Qué hace la función subList()?
La función subList() retorna una porción de la lista.

¿Observó algún cambio en el comportamiento del programa al cambiar la implementación del
parámetro “ARRAY_LIST” a “SINGLE_LINKED”? 
Al cambiar de ARRAY_LIST a SINGLE_LINKED, no se observan mayores cambios en el funcionamiento del programa.
El mayor cambio se da en el tiempo de ejecución del programa que es más ... con el SINGLE_LINKED.


